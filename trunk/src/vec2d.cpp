#include "vec2d.h"

/*
#include "macros.h"
#include <cstdio>


vec2d vec2d::normalized(){
  double len=length();
  vec2d ret;
  if(len==0.0){
    MSGBOX(L"Normalizing a zero-length vector!");
    ret.set(0.,0.);
  }else{
    ret.set(x/len,y/len);
  }
  return ret;
}
*/
